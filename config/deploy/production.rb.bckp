# config/deploy/production.rb

#server "hpc-sandbox.neb.com", user: "seq-shepherd", roles: %w{ngs_agg}
#server "hpc-sandbox.neb.com", user: "seq-shepherd", roles: %w{emseq}, primary: true

role :emseq %w[seq-shepherd@hpc-sandbox.neb.com]
role :ngs_agg %w[seq-shepherd@hpc-sandbox.neb.com]

# Define any custom SSH options
set :ssh_options, {
  forward_agent: true,
  auth_methods: %w(publickey),
  keys: %w(~/.ssh/id_rsa ~/.ssh/id_neb)
}
